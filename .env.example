# ローカル開発用。値は .env.local にコピーして実際のキーを入れる（.env.local はgit管理外）。
# 未設定でも /blog 以外のページは問題なく動く。

# microCMS（ブログ取得用）
MICROCMS_SERVICE_DOMAIN=
MICROCMS_API_KEY=

# 以下はローカルには置かない。Cloudflare Workers の secret としてのみ存在する
# （`wrangler secret put DISCORD_WEBHOOK_URL` で設定。コードにもリポジトリにも書かない）
# DISCORD_WEBHOOK_URL=
